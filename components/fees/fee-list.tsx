import type { FeeRow } from "@/lib/fees/data";
const label: Record<FeeRow["status"], string> = { unpaid: "Unpaid", partially_paid: "Partially paid", paid: "Paid", overdue: "Overdue" };
const money = new Intl.NumberFormat(undefined, { style: "currency", currency: "USD" });
export function FeeList({ fees, showStudent = false }: { fees: FeeRow[]; showStudent?: boolean }) { if (!fees.length) return <p className="rounded-lg border border-dashed p-5 text-sm text-muted-foreground">No fee records found.</p>; return <div className="space-y-3">{fees.map((fee) => <article key={fee.id} className="rounded-lg border bg-card p-4"><div className="flex flex-wrap items-start justify-between gap-3"><div><p className="font-semibold">{fee.feeType}</p><p className="mt-1 text-sm text-muted-foreground">{showStudent ? `${fee.studentName} · ${fee.admissionNumber} · ` : ""}{fee.academicYear}{fee.term ? ` · ${fee.term}` : ""}</p></div><span className="rounded-full bg-muted px-2.5 py-1 text-xs font-medium">{label[fee.status]}</span></div><div className="mt-4 grid gap-2 text-sm sm:grid-cols-4"><p><span className="block text-xs text-muted-foreground">Due</span>{money.format(fee.amountDue)}</p><p><span className="block text-xs text-muted-foreground">Paid</span>{money.format(fee.amountPaid)}</p><p><span className="block text-xs text-muted-foreground">Balance</span>{money.format(fee.balance)}</p><p><span className="block text-xs text-muted-foreground">Due date</span>{fee.dueDate}</p></div>{fee.payments.length ? <p className="mt-3 text-xs text-muted-foreground">Payments: {fee.payments.map((payment) => `${payment.receiptNumber} (${money.format(payment.amount)})`).join(", ")}</p> : null}</article>)}</div>; }
