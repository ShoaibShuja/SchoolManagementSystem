import { StatusBadge } from "@/components/shared/status-badge";
import { EmptyState } from "@/components/shared/states";
import type { PersonalAttendance } from "@/lib/attendance/types";

export function PersonalAttendanceView({ attendance }: { attendance: PersonalAttendance }) { return <section className="space-y-5"><div className="grid gap-px overflow-hidden rounded-lg border bg-border sm:grid-cols-3"><div className="bg-card p-4"><p className="text-xs text-muted-foreground">Attendance rate</p><p className="mt-2 text-2xl font-semibold">{attendance.summary.rate}%</p></div><div className="bg-card p-4"><p className="text-xs text-muted-foreground">Present or approved</p><p className="mt-2 text-2xl font-semibold">{attendance.summary.present + attendance.summary.late + attendance.summary.excused}</p></div><div className="bg-card p-4"><p className="text-xs text-muted-foreground">Absent</p><p className="mt-2 text-2xl font-semibold">{attendance.summary.absent}</p></div></div>{attendance.recent.length ? <div className="overflow-hidden rounded-lg border bg-card"><ul className="divide-y">{attendance.recent.map((record) => <li key={record.id} className="flex items-center justify-between gap-3 p-4"><div><p className="font-medium">{record.date}</p><p className="text-sm text-muted-foreground">{record.remarks ?? "No note"}</p></div><StatusBadge status={record.status} /></li>)}</ul></div> : <EmptyState title="No attendance records yet" description="Your attendance history will appear here once it has been marked." />}</section>; }
