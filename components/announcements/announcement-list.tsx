import type { Announcement } from "@/lib/announcements/types";
export function AnnouncementList({ announcements, empty = "No announcements are available." }: { announcements: Announcement[]; empty?: string }) { if (!announcements.length) return <p className="rounded-lg border border-dashed p-5 text-sm text-muted-foreground">{empty}</p>; return <div className="space-y-3">{announcements.map((item) => <article key={item.id} className="rounded-lg border bg-card p-4"><div className="flex flex-wrap justify-between gap-2"><h2 className="font-semibold">{item.title}</h2><span className="text-xs font-medium uppercase tracking-wide text-muted-foreground">{item.publishedOn ?? item.status}</span></div><p className="mt-2 whitespace-pre-wrap text-sm leading-6 text-muted-foreground">{item.body}</p>{item.expiresOn ? <p className="mt-3 text-xs text-muted-foreground">Available until {item.expiresOn}</p> : null}</article>)}</div>; }
