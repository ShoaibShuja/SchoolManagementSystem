import { NextRequest, NextResponse } from "next/server";
import { apiError, requireAdminApi } from "@/lib/admin/api";
import { deleteAcademicYear, deleteAssignment, deleteSubject, deleteTerm, deleteTimetableEntry, updateAcademicYear, updateSubject, updateTerm, updateTimetableEntry } from "@/lib/academics/data";
import { academicYearSchema, subjectSchema, termSchema, timetableSchema } from "@/lib/academics/schemas";

export async function PUT(request: NextRequest, { params }: { params: Promise<{ resource: string; id: string }> }) { const denied = await requireAdminApi(); if (denied) return denied; try { const { resource, id } = await params; const body = await request.json(); if (resource === "years") await updateAcademicYear(id, academicYearSchema.parse(body)); else if (resource === "terms") await updateTerm(id, termSchema.parse(body)); else if (resource === "subjects") await updateSubject(id, subjectSchema.parse(body)); else if (resource === "timetable") await updateTimetableEntry(id, timetableSchema.parse(body)); else return NextResponse.json({ error: "This record cannot be edited here." }, { status: 404 }); return NextResponse.json({ ok: true }); } catch (error) { return apiError(error); } }
export async function DELETE(_: NextRequest, { params }: { params: Promise<{ resource: string; id: string }> }) { const denied = await requireAdminApi(); if (denied) return denied; try { const { resource, id } = await params; if (resource === "years") await deleteAcademicYear(id); else if (resource === "terms") await deleteTerm(id); else if (resource === "subjects") await deleteSubject(id); else if (resource === "assignments") await deleteAssignment(id); else if (resource === "timetable") await deleteTimetableEntry(id); else return NextResponse.json({ error: "Unknown academic resource." }, { status: 404 }); return NextResponse.json({ ok: true }); } catch (error) { return apiError(error); } }
